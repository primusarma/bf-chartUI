var transformNumber = require('../../../src/scripts/transform/series');

describe('transformSeries module can ', function () {

    it('sum an array of numbers', function () {

        //expect(transformNumber(1)).toBe(1);

    });

});